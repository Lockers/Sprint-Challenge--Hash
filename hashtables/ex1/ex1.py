def get_indices_of_item_weights(weights, length, limit):

    hashDict = {}
    for i in range(length):
        # val = hashDict[limit - weights]
        print(i)
        # if hashDict[i] != None:
        #     print(hashDict.items())
        #     return (i, hashDict[i])
        # else:
        hashDict[weights[key]] = limit
        print(hashDict.items())
    return None
    
get_indices_of_item_weights(1, 3, 5)
